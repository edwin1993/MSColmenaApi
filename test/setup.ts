import { Test } from '@nestjs/testing';

// Configuración global para tests
beforeAll(async () => {
  // Configuraciones globales si son necesarias
});

afterAll(async () => {
  // Limpieza global si es necesaria
}); 